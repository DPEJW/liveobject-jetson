# liveobject — Blackwell / GB10 (CUDA 13) Python stack.
#
# Verified on: NVIDIA GB10 (Grace-Blackwell, sm_121), Ubuntu 24.04, CUDA 13.0,
# driver 580, Python 3.12. Install into a venv:
#
#     python3 -m venv .venv && source .venv/bin/activate
#     pip install -U pip wheel
#     pip install -r requirements-blackwell.txt \
#         --extra-index-url https://download.pytorch.org/whl/cu130
#
# torch's +cu130 build is only on the PyTorch index, hence --extra-index-url.
# (The Jetson/Orin stack — pypylon, pycuda, numpy 1.24 — is intentionally NOT
# here: no Basler camera on this box, and pycuda won't build against CUDA 13.
# The camera is the network Reolink over RTSP; CUDA memory ops use cuda-python.)

# --- inference runtime (required to run the app) ---
tensorrt-cu13==11.1.0.106
cuda-python==13.3.1          # cuda.bindings.runtime — replaces pycuda
numpy==2.5.0
opencv-python==5.0.0.93      # built WITH FFmpeg (RTSP decode), no GStreamer needed
flask==3.1.3
psutil==7.2.2
requests==2.34.2

# --- model export + on-device retrain (needed to build/rebuild engines) ---
torch==2.12.1+cu130
torchvision==0.27.1
ultralytics==8.4.84
onnx==1.22.0
onnxslim==0.1.94
