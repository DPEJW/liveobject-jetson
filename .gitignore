# Python
__pycache__/
*.py[cod]

# Runtime artifacts
snapshots/
*.log
hailort*.log

# Model weights / engines (large; .engine is specific to this TensorRT/GPU,
# ONNX/.pt are regenerable — see README "Building the detection engines")
models/*.engine
models/*.onnx
models/*.pt
